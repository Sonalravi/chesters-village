// /chester — Chester's tribute page
// Content column max 640px, full-bleed photos punctuate prose.
// No parallax, no scroll-triggered animations. Reads like a book.
// Font: Fraunces for both display and body text.

export default function ChesterPage() {
  return (
    <main className="flex-1">
      {/* Chester watermark / header */}
      <section />

      {/* Full story: Chester and Sona's journey (India to SF) */}
      <section />

      {/* Bucket list moments */}
      <section />

      {/* Chester photo gallery */}
      <section />
    </main>
  );
}
