// /pups — Village directory
// Grid of member dogs, pup name below each card in Caveat.
// Desktop: hover reveals neighborhood/one-word vibe caption.
// Mobile: static, no hover state.
// Paw print icon as list markers.

export default function PupsPage() {
  return (
    <main className="flex-1 max-w-[1200px] mx-auto px-4">
      {/* Pup grid */}
      <section />
    </main>
  );
}
